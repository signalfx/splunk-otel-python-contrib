apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: litellm-secrets
data:
  OPENAI_API_KEY: # replace with your base64-encoded OpenAI API key
    # To encode your API key, run:
    # echo -n "your-api-key-here" | base64
    # Then paste the output above (remove this comment line)

# Minimal dependencies to run examples (like examples/traceloop_example.py)
# Usage: pip install -r requirements-examples.txt
# Pinned 2026-04-28 - HYBIM-586: pinned to prevent supply-chain attacks via unpinned AI/ML packages

-e ../opentelemetry-util-genai
-e .

# OpenTelemetry SDK pieces used in the example
opentelemetry-sdk==1.38.0
traceloop-sdk==0.60.0
python-dotenv==1.2.2
openai==2.32.0
# (ConsoleSpanExporter is in the SDK extras; no additional exporter deps needed.)

# Auto-enable Langsmith span processor on package installation
# This .pth file automatically imports the langsmith module when the package is installed,
# triggering the _auto_enable() function that registers the LangsmithSpanProcessor
# with the global TracerProvider.
import opentelemetry.util.genai.langsmith

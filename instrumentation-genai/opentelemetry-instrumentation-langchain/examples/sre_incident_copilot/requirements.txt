langchain>=1.0.0
langchain-openai>=1.0.0
# Pin upper bound in lockstep with splunk-otel-instrumentation-langchain's
# `instruments` constraint (>=0.4.0, <=1.1.6).  langgraph 1.1.7 introduced a
# breaking change in callback dispatch that prevents the LangchainCallbackHandler
# from firing, so util-genai emits no workflow/agent/tool spans (see PR #280).
langgraph>=1.0.0,<=1.1.6
langgraph-checkpoint-sqlite>=3.0.0
python-dotenv>=1.0.0
# Pin fastmcp to the version verified end-to-end against
# splunk-otel-instrumentation-fastmcp 0.2.1 + splunk-otel-util-genai 0.1.13
# (traces + metrics shipped to Splunk Observability Cloud US1, MCP client
# transport + server-side MCPOperation spans emit correctly).
fastmcp==3.2.4
httpx>=0.27.0
chromadb>=0.4.0
langchain-community>=0.3.0
pydantic>=2.0.0
nest-asyncio>=1.6.0


[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: marks tests as integration tests (require running Weaviate instance)
addopts = 
    -v
    --strict-markers
    --tb=short
